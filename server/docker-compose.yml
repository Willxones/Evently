services:
    backend:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - '3000:3000'
        env_file: .env
        volumes:
            - .:/app
            - /app/node_modules # Prevents host node_modules from overriding container's
        command: ['npm', 'run', 'dev']
